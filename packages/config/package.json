{
  "name": "@control-os/config",
  "version": "0.1.0",
  "private": true,
  "description": "Configuração compartilhada do monorepo (TypeScript base). Consumida por apps/web via extends relativo.",
  "files": [
    "tsconfig.base.json"
  ]
}
